name: Naukri Job Agent

# ─────────────────────────────────────────────────────────────────────────────
#  SCHEDULE  (IST = UTC + 5:30)
#   10:00 AM IST  →  04:30 UTC
#   10:00 PM IST  →  16:30 UTC
# ─────────────────────────────────────────────────────────────────────────────
on:
  schedule:
    # - cron: '30 4 * * *'    # 10:00 AM IST
    # - cron: '30 16 * * *'   # 10:00 PM IST
    - cron: '30 */3 * * *'   # Every 3 hours at HH:30 UTC
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Install Chrome dependencies
        run: |
          sudo apt-get update -q
          sudo apt-get install -y --no-install-recommends \
            google-chrome-stable \
            xvfb \
            libatk1.0-0 \
            libatk-bridge2.0-0 \
            libgtk-3-0 \
            libnss3 \
            libxss1
          sudo apt-get install -y libasound2 || sudo apt-get install -y libasound2t64 || true
          echo "Chrome version: $(google-chrome --version)"

      - name: Remove misplaced duplicate source files
        run: |
          rm -f src/main/java/com/jobagent/agent/WhatsAppNotifier.java
          echo "Source tree clean."

      - name: Copy application.properties to working directory
        run: |
          cp src/main/resources/application.properties ./application.properties
          echo "── Verifying config keys ──"
          cut -d'=' -f1 ./application.properties

      - name: Build with Maven
        run: |
          mvn -B package -DskipTests --no-transfer-progress
          echo "── Built jars ──"
          ls -lh target/*.jar 2>/dev/null || echo "No jars found in target/"

      - name: Locate jar
        run: |
          JAR=$(ls target/*.jar 2>/dev/null | grep -v 'original-' | head -1)
          if [ -z "$JAR" ]; then
            echo "ERROR: No runnable jar found in target/"
            ls -lh target/ || true
            exit 1
          fi
          echo "JAR_PATH=$JAR" >> $GITHUB_ENV
          echo "Runnable jar: $JAR"

      # ── 🚀 Run Agent ───────────────────────────────────────────────────────
      #
      #  application.properties has NA values for all secrets.
      #  AppConfig.java sees NA → reads from env vars below → uses real values.
      #  This is exactly the logic we built in AppConfig.fallbackToEnv().
      #
      - name: Run Naukri Job Agent
        run: |
          echo "Starting Xvfb display..."
          Xvfb :99 -screen 0 1920x1080x24 -ac &
          XVFB_PID=$!
          sleep 3

          if ! kill -0 $XVFB_PID 2>/dev/null; then
            echo "ERROR: Xvfb failed to start"
            exit 1
          fi
          echo "Xvfb running (PID=$XVFB_PID)"
          echo "Running jar: $JAR_PATH"

          java \
            -Xmx512m \
            -Dfile.encoding=UTF-8 \
            -jar "$JAR_PATH" \
            --once 2>&1 | tee agent_run.log
          EXIT_CODE=${PIPESTATUS[0]}

          kill $XVFB_PID 2>/dev/null || true

          if [ $EXIT_CODE -ne 0 ]; then
            echo "❌ Agent exited with code $EXIT_CODE"
            exit $EXIT_CODE
          else
            echo "✅ Agent completed successfully"
          fi
        env:
          DISPLAY: ':99'
          # ── 🔐 Secrets passed as env vars to the JVM process ──────────────
          # AppConfig.java: sees NA in properties → reads these → uses real values
          NAUKRI_USERNAME:    ${{ secrets.NAUKRI_USERNAME }}
          NAUKRI_PASSWORD:    ${{ secrets.NAUKRI_PASSWORD }}
          GROQ_API_KEYS:      ${{ secrets.GROQ_API_KEYS }}
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN:  ${{ secrets.TWILIO_AUTH_TOKEN }}
          WHATSAPP_TO:        ${{ secrets.WHATSAPP_TO }}
          WHATSAPP_FROM:      ${{ secrets.WHATSAPP_FROM }}

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: naukri-results-${{ github.run_number }}
          path: |
            naukri_job_matches.txt
            naukri_applied_jobs.log
            agent_run.log
            error_*.png
          retention-days: 7